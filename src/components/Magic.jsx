import Button from './Button'
import Input from './Input'

const Magic = ({ball}) => {
   

    return (
        <div>
            <h1>{ball}</h1>
            <Input/>
            
        </div>
    )
    }

export default Magic
